# Interest Equalisation Scheme SEO Implementation

## Page

- URL: `/services/interest-equalisation-scheme`
- Component: `src/pages/CloudDeskIES.jsx`
- Supporting files:
  - `src/components/CloudDeskIES/QuickForm.jsx`
  - `src/config/seoConfig.js`

## Objective

This implementation rebuilds the Interest Equalisation Scheme page into a stronger SEO-focused exporter finance page designed to improve crawling, indexability, topical authority, and conversion quality for users searching for:

- `interest equalisation scheme consultant India`
- `interest equalisation scheme for exporters`
- `export credit interest subvention support`
- `DGFT interest subvention support`
- `packing credit interest subsidy`
- `post shipment export credit support`
- `export promotion mission interest subvention`

The page now targets both legacy `Interest Equalisation Scheme` search demand and current DGFT `interest subvention` workflow intent, which makes the content more useful and less vulnerable to becoming stale.

## Implemented SEO Improvements

### 1. Metadata optimization

Updated the page title to:

`Interest Equalisation Scheme Consultant India | Export Credit Interest Subvention Support, DGFT and Bank Coordination | EXIMINQ`

Why it helps:

- Leads with the primary commercial keyword.
- Adds high-intent secondary entities such as `export credit interest subvention`, `DGFT`, and `bank coordination`.
- Aligns the page with how exporters actually search when they need implementation help rather than only scheme awareness.

Updated the meta description to cover:

- Interest Equalisation Scheme consultant
- export credit interest subvention support
- DGFT workflow
- bank coordination
- MSME eligibility review
- packing credit
- post-shipment credit
- current EPM-linked guidance

Why it helps:

- Improves relevance for both transactional and informational search intent.
- Gives Google clearer snippet context and improves CTR potential.

Added:

- `robots` directive: `index, follow, max-image-preview:large`
- broader keyword coverage
- improved `og` and `twitter` metadata
- canonical tag pointing to the live route

### 2. Full SEO-first page rebuild

The old page was replaced with a more accurate, search-intent-driven structure.

Why it helps:

- Reduces thin-content and outdated-policy risk.
- Creates a cleaner semantic structure for search engines.
- Improves topical fit for exporter finance, DGFT workflow, and scheme advisory intent.

### 3. Structured data

Added JSON-LD schema:

- `WebPage`
- `BreadcrumbList`
- `Service`
- `FAQPage`

Why it helps:

- Improves Google's understanding of page purpose and site hierarchy.
- Reinforces that this is a specialised export-finance advisory service page.
- Supports stronger semantic processing during crawling and indexing.

### 4. Content depth and keyword coverage

Expanded the page to naturally cover:

- interest equalisation scheme
- export credit interest subvention
- packing credit support
- post-shipment export credit support
- DGFT workflow
- bank coordination
- MSME review
- Export Promotion Mission context

Why it helps:

- Expands semantic coverage across short-tail and long-tail keywords.
- Better satisfies informational, evaluative, and transactional search intent.
- Improves topical completeness compared with a generic scheme explainer.

### 5. Search-intent sections added

Added:

- `Overview`
- `Benefits`
- `Eligibility`
- `Documents Required`
- `Timelines`
- `Process Flow`
- `Official References`
- `Related Services`
- `FAQs`
- `Conversion-Focused CTA`

Why it helps:

- Aligns with the real exporter journey from scheme discovery to execution.
- Improves engagement and depth.
- Helps the page answer more of the queries users ask before contacting a consultant.

### 6. Accuracy and freshness improvements

The page was re-positioned to reflect the fact that many users still search for `Interest Equalisation Scheme`, while the current official guidance flow overlaps with `interest subvention support` under DGFT's Export Promotion Mission.

Why it helps:

- Improves trustworthiness.
- Reduces risk from outdated static claims.
- Supports stronger E-E-A-T by anchoring the page in official references and current policy framing.

## Crawling, Indexing, and Ranking Impact

### Crawling

The page now uses:

- a clear canonical tag
- clean HTML structure
- internal links to related services
- semantic headings
- crawl-friendly content without hidden critical text

This makes it easier for Googlebot to discover and interpret the page.

### Indexing

The updated content gives Google enough topical depth to justify indexing:

- stronger entity coverage
- more complete service information
- FAQ schema
- breadcrumb schema
- clearer title and description

This helps reduce the risk of the page being treated as thin or low-value.

### Ranking

The ranking improvements come from:

- better keyword targeting
- higher topical completeness
- improved search-intent alignment
- stronger internal linking
- structured data
- official-reference-backed trust signals

## Technical SEO Notes

- Canonical set to the live route: `https://eximinq.in/services/interest-equalisation-scheme`
- Route metadata aligned in `seoConfig.js`
- JSON-LD added directly in `Helmet`
- Page kept fully renderable for prerendering and static HTML generation
- No unnecessary dependency on hidden JS-only content for core copy

## Content Strategy Improvements

The new page intentionally combines:

- legacy keyword capture
- current policy interpretation
- transaction-focused service copy
- deeper exporter FAQs
- internal authority links
- practical CTA placement

This gives the page a stronger chance of ranking for both direct service queries and broader exporter finance information queries.

## Recommended Next Steps

### High Priority

- Link to this page from service hubs, DGFT pages, exporter-finance pages, and relevant blog or update pages using anchor text such as:
  - `Interest Equalisation Scheme consultant`
  - `export credit interest subvention support`
  - `interest subvention for exporters`

- Ensure the route appears in the live sitemap with the canonical version.

### Medium Priority

- Add a supporting article or regulatory update page on recent DGFT Export Promotion Mission interest subvention changes.
- Add testimonial or case-result content to strengthen E-E-A-T and conversion quality.

### Low Priority

- Add original visual assets or workflow diagrams with descriptive alt text.
- Add supporting comparison content between export finance support, duty drawback, EPCG, and Advance Authorisation.

## Verification Summary

Verify after build and deployment:

- prerendered HTML contains the new title and meta description
- canonical tag matches the intended live route
- FAQ schema and breadcrumb schema are present
- internal links resolve correctly
- page is included in sitemap and is indexable
